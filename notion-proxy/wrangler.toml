name = "camden-notion-proxy"
main = "cloudflare-worker.js"
compatibility_date = "2025-09-03"

# NOTION_TOKEN is set as a secret in the Cloudflare dashboard and is
# preserved across `wrangler deploy` — never put the token in this file.
